#pragma once
#include <GLFW/glfw3.h>
#include <string>
#include <vector>
#include <iostream>

namespace GPUHelpers 
{
	inline GLuint loadCommputeShader(const char* path)
	{
		/*std::ifstream file;

		if(!file.is_open()) 
		{

		}*/
	}
}